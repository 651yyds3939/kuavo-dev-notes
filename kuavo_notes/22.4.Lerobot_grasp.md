# 📖 22.4 VLA 抓取 — LeRobot 模仿学习数据采集

> **文档定位**：Kuavo v49 **LeRobot v3.0 专家示范 → 打包 → 在自己电脑 GPU 训练**完整操作手册（从开机点火 → 采数 → 打包 → 验证 → **ACT 试训**）。  
> **阅读顺序**：本文共 **10 章**，按**项目时间线**编排（概述 → 采数 → 打包 → 训练 → 评估 → 答疑）；**§1.6** 为 Day 0~3 总览。  
> **快速跳转**：§1 概述与架构 · §1.5 三台分工 · §3 采数 SOP · §4.2.4 删数据/jsonl · §5 打包 · §7 训练 · **§8 离线评估（必读）** · §10 答疑  
> **22.3** 讲 MCP 大模型 Tool Call → [`22.3.MCP_VLA_grasp.md`](./22.3.MCP_VLA_grasp.md)  
> **22.2** 讲行为树 Baseline → [`22.2.tree_VLA_grasp.md`](./22.2.tree_VLA_grasp.md)（九终端架构、TTS、ASR 等通用踩坑仍有效）  
> **夹爪硬件** → [`20.gripper_issue.md`](./20.gripper_issue.md)  
> **红线**：不修改 `vla_bt_daemon.py` / `bt/`；**执行入口**（22.2 终端 5 / 22.3 MCP 桥 / 22.4 终端 7 收割机）**三选一**。

---

## 目录

- [第 1 章：项目概述与系统架构](#ch1)
- [第 2 章：环境准备（Day 0）](#ch2)
- [第 3 章：首次冒烟采数（Day 1）](#ch3)
- [第 4 章：Episode 管理与批量采数（Day 2）](#ch4)
- [第 5 章：raw 传输与 Orin 打包](#ch5)
- [第 6 章：pack 数据集拷到自己电脑](#ch6)
- [第 7 章：在自己电脑 GPU 训练（LeRobot ACT）](#ch7)
- [第 8 章：模型离线评估与实机部署](#ch8)
- [第 9 章：进度与参考](#ch9)
- [第 10 章：常见问答（2026-06 答疑）](#ch10)

---

<a id="ch1"></a>
## 第 1 章：项目概述与系统架构

> 先弄清采什么、三台机器分工、整条流水线。


### 1.1 系统架构图（三台机器 · 数据流）

> **三台机器分工**：下位机 **动起来 + 录 raw**；上位机 **眼睛（相机/YOLO）+ 离线 pack**；自己电脑 **GPU 训练 + 离线评估**（实机推理节点尚未实现）。

```text
========================================================================================================
              📦 LeRobot 模仿学习数据采集 · 三台机器全流程架构图 (8 终端 + 三阶段数据流)
========================================================================================================

  [ 自己电脑 PC · 外接 NVIDIA GPU 训练机 ]  -- (不参与采数；只接收 pack 数据集)
========================================================================================================
       |
       |  tar / rsync / U 盘  <-- 只拷 pack 文件夹 (~9GB)，不拷 raw (~62GB)
       ^
       |
[阶段 C] lerobot-train ACT  -->  outputs/train/.../checkpoints/
         离线 MAE 评估 (§8 predict_action_chunk)  -->  实机推理节点 (尚未实现)
       |
========================================================================================================
  [ 上位机 Orin · 192.168.26.12 ]  -- (相机 + YOLO + 离线 pack)
========================================================================================================
       |                                    |
[终端 5] 头部相机 RGB 流              [终端 6] YOLO + TF2 视觉
 load_robot_head.launch                 yolo_box_segment_ros_TF2.py
 /camera/color/image_raw                /vla/yolo_target (瓶子 TF 坐标)
       |                                    |
       +------------------ ROS 图像/坐标 ---+------------------->  下位机 harvester 订阅
       |                                    |
[终端 8] rqt_image_view (可选，调试用)      |
       |                                    |
=======|====================================|===========================================================
       |         局域网 ROS 总线 (192.168.26.x)  +  rsync/scp 文件传输
=======|====================================|===========================================================
       |                                    |
       v                                    v
========================================================================================================
  [ 下位机 NUC · 192.168.26.1 · ROS Master · root/lab ]
========================================================================================================
[终端 1] WBC 站稳 (load_kuavo_real.launch)
       |
[终端 2] move_group.launch  -->  /compute_ik  (手臂规划)
       |
[终端 3] look_down.py  -->  低头看桌面
       |
[终端 4] kuavo_state_publisher.py  -->  /joint_states (14 轴)  ----+
       |                                                            |
       +-------------------- 感知 + 状态 汇入 ----------------------+
       |                                                            |
[终端 7] lerobot_data_harvester.py  (⭐ 最后开)  <-----------------+
         |-- 调 moveit_auto_grasp 执行整条抓取 (专家示范)
         |-- 25Hz 录：相机图 + joint_states + 下发 action
         v
[阶段 A] lerobot_kuavo_grasp_raw_batch2/   (~62GB)
         episodes/ep_*.npz  +  meta/episodes.jsonl
         |  删失手 / 续采 / 重建 jsonl (§4.2.4)
         |  rsync raw 到上位机
         v
[阶段 B] [上位机] lerobot_pack_dataset.py  -->  lerobot_kuavo_grasp_batch2/  (~9GB, LeRobot v3)
         |  640×400 轻量化 pack · 88 ep · 27137 frames
         |  tar / rsync 拷到 PC
         v
[阶段 C] [PC] lerobot-train ACT  -->  checkpoints/050000/pretrained_model/
         离线 MAE ≈ 0.011 rad (§8)  -->  未来：实机开环/闭环抓瓶 (§8.6~7)
========================================================================================================
```

#### 1.1.1 流程图导读

1. **纵向三阶段**：**阶段 A** 下位机采 raw → **阶段 B** 上位机 pack → **阶段 C** PC 训练/评估。训练**不需要** ~62GB raw，只拷 pack 后的 `lerobot_kuavo_grasp_batch2/`。
2. **采数时横向协作**：上位机 Orin 提供 **相机 + YOLO**（终端 5/6），经 **ROS** 发给下位机 NUC 的 **harvester**（终端 7）；NUC 同时跑 WBC/MoveIt/关节状态（终端 1~4）。
3. **点火顺序**：NUC **1→2→3→4** → Orin **5→6** → NUC **7（最后）**；详见 **§3.1**。
4. **当前进度（2026-07）**：88 ep pack ✅ · ACT 50K step ✅ · 正确离线 MAE ≈ 0.011 rad ✅ · 实机推理 ⬜。

**三阶段数据流（与 §1.4 一致）：**

```text
【阶段 A · 下位机采 raw】  harvester --raw-only  →  lerobot_kuavo_grasp_raw_batch2/  (~62GB)
【阶段 B · 上位机 pack】   pack_dataset.py       →  lerobot_kuavo_grasp_batch2/       (~9GB, LeRobot v3)
【阶段 C · 自己电脑训练】  只拷 pack 文件夹       →  lerobot-train ACT               →  checkpoints/
```

| 机器 | IP | 采数时 | 采数后 | 训练 |
|------|-----|--------|--------|:----:|
| **下位机 NUC** | `192.168.26.1` | WBC / MoveIt / harvester 录 npz | 存 raw、删坏条、续采、重建 jsonl、可当中转 | ❌ |
| **上位机 Orin** | `192.168.26.12` | 相机 + YOLO 提供感知 | rsync raw → **pack 成 LeRobot** | ❌ |
| **自己电脑 PC** | — | 不参与 | 接收 pack 数据集 | ✅ ACT 训练 + §8 离线评估 |

> 采数时的 **ROS 话题拓扑**（八终端 ASCII 简图）见 **§3.1.4**；训练/评估命令见 **§7**、**§8**。


### 1.2 一句话结论


| 项目 | 说明 |
|------|------|
| **采什么** | 机器人**自己抓水瓶**时的图像 + 14 轴 state/action |
| **下位机** `192.168.26.1` | `lerobot_data_harvester.py --raw-only` 真机录 npz |
| **上位机** `192.168.26.12` | `lerobot_pack_dataset.py` 离线打包 LeRobot v3 |
| **自己电脑** | 采数/打包 **不需要**；**ACT / VLA 训练在这里做**（§7） |
| **2026-07 状态** | ✅ batch2 **88 ep** 轻量化 pack → PC **ACT 50K step** 训完 → **正确离线 MAE≈0.011 rad** → **下一步：实机开环推理**（§8.6~7） |

---

### 1.3 采集什么数据


#### 1.3.1 用途

**模仿学习（Imitation Learning）** 专家示范：Episode 内全自动抓取，Episode 间人工挪瓶。

#### 1.3.2 每帧字段

| 字段 | 来源 | 实机格式 |
|------|------|----------|
| `observation.images.head` | `/camera/color/image_raw` | 1280×800×3 uint8 |
| `observation.state` | `current_joints_rad` | 14 轴 float32 弧度 |
| `action` | `last_commanded_joints_rad` | 14 轴 float32 弧度 |
| `task` | 脚本生成 | `"grasp bottle epN"` |

LeRobot 加载后 image 为 `torch.Size([3, 800, 1280])`（CHW）。

#### 1.3.3 一次 Episode 流程

```text
YOLO 10 帧采点 → 曲肘护胸 → 预瞄(12cm 外) → 水平插入 → 闭爪
    → 垂直抬升（优先 22cm；IK 无解时 18→14→10cm 递减重试）→ 肩膀外摆 75° → 曲肘收手 → init 下垂 → 开爪释放
```

典型 **~1200 帧 @50Hz ≈ 25s**；`raw_y > 0` 左手，`raw_y < 0` 右手。

---

### 1.4 两阶段流水线

```text
【下位机 采 raw】  harvester --raw-only  →  lerobot_kuavo_grasp_raw_batch2/
【上位机 打包】    pack_dataset.py       →  lerobot_kuavo_grasp_batch2/  (LeRobot v3)
【自己电脑 训练】  rsync/scp 只拷 pack 文件夹 →  lerobot-train ACT  →  outputs/train/...
```

> 训练**不需要** ~62GB raw，只拷 **pack 后的** `lerobot_kuavo_grasp_batch2/`。完整命令见 **§7**。


### 1.5 下位机 / 上位机 / 自己电脑各自做什么

下位机 / 上位机 / 自己电脑各自做什么

| | **下位机** `192.168.26.1` | **上位机** `192.168.26.12` | **自己电脑** |
|--|---------------------------|----------------------------|--------------|
| **定位** | 机器人主控 / ROS Master | 头部相机 + 视觉 | 外接 NVIDIA GPU 训练机 |
| **采数时** | 控制 + 录 raw | 提供图像 + 瓶子位置 | 不参与 |
| **采数后** | 存 raw、删坏条、续采、可当中转 | **pack 成 LeRobot 数据集** | **ACT 训练** |
| **训练** | ❌ | ❌ | ✅ |

**采数阶段（机器人上电，ROS 联网）**

| 机器 | 终端 | 做什么 |
|------|:----:|--------|
| **下位机** | 1 | WBC 站起来 |
| **下位机** | 2 | `move_group.launch` 手臂规划 / IK |
| **下位机** | 3 | `look_down.py` 低头看桌面 |
| **下位机** | 4 | `kuavo_state_publisher.py` → `/joint_states`（14 轴） |
| **上位机** | 5 | `load_robot_head.launch` → `/camera/color/image_raw` |
| **上位机** | 6 | YOLO → `/vla/yolo_target`（瓶子 TF 位置） |
| **下位机** | **7** | **`lerobot_data_harvester.py`**：调 `moveit_auto_grasp` 抓瓶 + 25Hz 录 npz |
| **上位机** | 8（可选） | `rqt_image_view` 看图 |

**一次 Episode 里谁配合谁：**

```text
上位机 YOLO 报瓶子位置 ──ROS──→ 下位机 moveit_auto_grasp 执行整条抓取
                                      ↓ 同时
下位机 harvester 收：相机图 + 关节状态 + 下发动作 → 写 ep_XXXXXX.npz
                                      ↓
Episode 间你人工挪瓶；Episode 内全自动
```

**harvester 录的每帧：**

- 图像：订阅上位机 **`/camera/color/image_raw`**
- `observation.state`：订阅 **`/joint_states`**
- `action`：harvester 自己下发的关节指令

**采数结束后**

| 机器 | 产物 / 操作 |
|------|-------------|
| **下位机** | `lerobot_kuavo_grasp_raw_batch2/`（~62GB，35 条 npz）；删失手、续采、重建 jsonl |
| **上位机** | rsync raw → 跑 `lerobot_pack_dataset.py` → `lerobot_kuavo_grasp_batch2/` |
| **自己电脑** | 只拷 **pack 文件夹** → `lerobot-train`（§7） |

**一句话：** 下位机 = **动起来 + 录 raw**；上位机 = **眼睛（相机/YOLO）+ 整理成训练格式**；自己电脑 = **GPU 学策略**。

---


### 1.6 项目时间线总览


#### 1.6.1 Day 0 — 环境准备（一次性）

1. Orin 装 conda + lerobot（§5.2.1）
2. NUC 仓库已有 `mcp/lerobot_data_harvester.py`、`lerobot_pack_dataset.py`
3. 夹爪标定正常

#### 1.6.2 Day 1 — 冒烟（1 ep 采 + 打包 + 验证）

**NUC 按 §3.2.2 → 3.2.5 → Orin §3.2.6 → 3.2.7 → NUC §3.2.8（`--episodes 1`）**

**Orin 打包 §5.2.3 + 验证 §5.2.4**

#### 1.6.3 Day 2 — 批量采数（20~100 ep）

**重新点火 §3.1**，**终端 7** 用 `--episodes 20 --no-compress --fps 25`

每条 ep 结束后等 `Episode N 成功`，再挪瓶。

#### 1.6.4 Day 3 — 在自己电脑 GPU 训练（§7 全文）

**前提**：上位机（或下位机）已有 pack 结果 `lerobot_kuavo_grasp_batch2/`。早期 smoke 为 35 ep / 10770 frames；**当前正式集为 88 ep / 27137 frames / 640×400**（§7.4.1）。

| 步骤 | 在哪做 | 做什么 |
|:----:|--------|--------|
| 1 | 自己电脑 | 从**下位机**或**上位机**拷 **pack 文件夹**（不拷 raw）→ §7.2；tar 解压见 **§6.2** |
| 2 | 自己电脑 | 装 conda + **CUDA 版 torch** + `lerobot` → §7.3 |
| 3 | 自己电脑 | 加载数据集自检 → §7.4 |
| 4 | 自己电脑 | **`lerobot-train` ACT 试训/正式训** → §7.5 / **§7.4.1（88 ep）** |
| 5 | 自己电脑 | 看 loss 下降、checkpoint 在 `outputs/train/...` → §7.6、§7.10 |
| 6 | 自己电脑 | **离线 MAE 评估（ACT 正确测法）** → **§8.4**（不必上真机） |

**Day 3 最小命令**（路径按你本机改）：

```bash
# --- 1) 拷数据集（在自己电脑上，能 SSH 下位机时）---
rsync -avP lab@192.168.26.1:/home/lab/lerobot_kuavo_grasp_batch2/ \
    ~/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2/

# --- 2) 环境（一次性）---
conda create -n lerobot python=3.12 -y && conda activate lerobot
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
pip install lerobot opencv-python-headless
unset PYTHONPATH
python -c "import torch; print('cuda:', torch.cuda.is_available())"

# --- 3) 训练 ---
export DATASET_ROOT="$HOME/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"
lerobot-train \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --policy.type=act \
  --policy.repo_id=local/kuavo_grasp_act \
  --policy.push_to_hub=false \
  --output_dir=outputs/train/kuavo_grasp_act_35ep \
  --job_name=kuavo_grasp_act \
  --policy.device=cuda \
  --steps=50000 \
  --batch_size=4 \
  --save_freq=5000 \
  --log_freq=100
```

传输慢见 **§6.2**（经下位机中转、tar.gz 压缩）。踩坑见 **§7.7**、监控见 **§7.10**、评估见 **§8**。

---

<a id="ch2"></a>
## 第 2 章：环境准备（Day 0）

> 一次性：脚本参数、harvester 选项、写盘权限。

### 2.1 Harvester CLI 参数

#### 2.1.1 参数补充（相对旧表新增/变更）

| 参数 | 默认 | 说明 |
|------|------|------|
| `--start-index` | `0` | 续采首 npz 序号，如已有到 ep_000029 则填 `30` |
| `--reposition-pause` | **`10.0`** | 挪瓶间隔（旧文档写 15s，以脚本默认为准） |

启动时会检查 `--output-dir` 可写；写盘结束打印**实际耗时**（秒）。

---

| 参数 | 默认 | 说明 |
|------|------|------|
| `--raw-only` | **开** | NUC 写 npz，不装 lerobot |
| `--episodes` | 100 | Episode 总数 |
| `--output-dir` | `~/...` | **必须绝对路径** `/home/lab/...` |
| `--start-index` | `0` | 续采首 npz 序号，如已有到 ep_000029 则填 `30` |
| `--fps` | 50 | 推荐 **25** 减半写盘与体积 |
| `--no-compress` | 关 | **推荐开**：写盘约 **3~5s/ep**（压缩模式 3~10min） |
| `--reposition-pause` | **`10.0`** | 挪瓶间隔（秒）；可酌情加长 |
| `--use-vision` / `--no-vision` | 开 | YOLO 10 帧采点 |
| `--min-frames` | 50 | 少于此视为失败不保存 |
| `--image-topic` | `/camera/color/image_raw` | 头部 RGB |

**推荐批量采数命令**：

```bash
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only \
    --episodes 20 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
    --fps 25 \
    --no-compress
```

**磁盘估算**：

| 模式 | 单 ep 大小 | 写盘时间 | 10 ep | 100 ep |
|------|-----------|----------|-------|--------|
| 50Hz + zlib | ~1.8GB | 3~10 min | ~1h | ~6~7h |
| 25Hz + `--no-compress` | ~1.5GB | **~3~5s** | ~1 min | ~10 min |

---
#### 2.1.2 写盘权限（lab 用户）

若 batch 目录曾用 **root** 采过，`lab` 用户写 npz 会 `PermissionError`。修复：

```bash
sudo chown -R lab:lab /home/lab/lerobot_kuavo_grasp_raw_batch2
```

harvester 已加**启动前写权限检查**，无权限会在点火前报错。下位机用 `lab` 或 `root` 均可，**输出目录属主须与运行用户一致**。

---

<a id="ch3"></a>
## 第 3 章：首次冒烟采数（Day 1）

> 八终端点火 → 跑 1 条 episode → 对照日志。

### 3.1 八终端点火 SOP

> **编号规则**：**终端 1~8 = 实际点火顺序**，不再使用「终端 S」或与 22.2 混用编号。  
> 与 22.2 对照见 §9.3（22.2 的「终端 5」≈ 本文 **终端 7**）。

> **与 22.2 的区别**  
> - 本文 **终端 7** = `lerobot_data_harvester.py`（22.2 终端 5 是 `vla_bt_daemon.py`）  
> - 本文 **终端 4** = `kuavo_state_publisher.py`（22.2 未写，**实测必开**）  
> - 本文 **终端 2** = `move_group.launch`（22.2 终端 2 是 `ik_node.launch`）  
> - 22.2 **终端 4 TTS**、**终端 9 语音** — LeRobot 采数 **不需要**

#### 3.1.1 点火顺序总表

| 终端 | 机器 | 用户 | 脚本 | 等待条件 |
|:----:|------|:----:|------|----------|
| **1** | NUC `192.168.26.1` | **root** | WBC 站起来 | 标定完成、关节响、**站稳** |
| **2** | NUC | **root** | `move_group.launch` | **`You can start planning now!`** |
| **3** | NUC | **root** | `look_down.py` | 头部**低头到位** |
| **4** | NUC | **root** | `kuavo_state_publisher.py` | `/joint_states` 有非零角 |
| **5** | Orin `192.168.26.12` | 本地 | `load_robot_head.launch` | `/camera/color/image_raw` 有 hz |
| **6** | Orin | 本地 | `yolo_box_segment_ros_TF2.py` | 刷 TF2_X/Y |
| **7** | NUC | **root** | **`lerobot_data_harvester.py`** ⭐最后 | 打印 `Episode 1 / N` |
| **8** | Orin | 本地 | `rqt_image_view` | **可选**，看图调试 |

**原则**：NUC **1→2→3→4** → Orin **5→6** → NUC **7（最后）**。

#### 3.1.2 点火速查（按顺序复制，详细命令见 §3.2）

```text
【NUC · root】cd /home/lab/kuavo-ros-opensource && source devel/setup.bash
  终端 1  roslaunch humanoid_controllers load_kuavo_real.launch cali:=true
  终端 2  roslaunch kuavo_arm_moveit_config move_group.launch
  终端 3  python3 src/demo/vla_grasp/look_down.py
  终端 4  python3 src/demo/vla_grasp/kuavo_state_publisher.py

【Orin · 本地】cd ~/kuavo_ros_application && source devel/setup.bash
  终端 5  roslaunch dynamic_biped load_robot_head.launch
  终端 6  python3 src/ros_vision/detection_industrial_yolo/yolo_box_object_detection/scripts/yolo_box_segment_ros_TF2.py

【NUC · root】确认终端 1~6 就绪后，最后开终端 7
  终端 7  python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
              --raw-only --episodes 10 \
              --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
              --fps 25 --no-compress

【Orin · 可选】
  终端 8  rqt_image_view
```

#### 3.1.3 网络与 SSH

| 你在哪 | 连哪台 | 命令 |
|--------|--------|------|
| Orin | NUC | `ssh lab@192.168.26.1` |
| NUC | Orin | `ssh leju_kuavo@192.168.26.12` |

两边 `ROS_MASTER_URI` 必须指向**同一 ROS Master**（通常 NUC `192.168.26.1:11311`）。

#### 3.1.4 架构图（LeRobot 采数版）

```text
Orin 192.168.26.12                          NUC 192.168.26.1
─────────────────                          ─────────────────
[5] load_robot_head.launch                  [1] load_kuavo_real.launch (WBC 站稳)
      │ /camera/color/image_raw ──ROS──→         │
[6] yolo → /vla/yolo_target ──ROS──→           [4] kuavo_state_publisher → /joint_states
                                               [2] move_group.launch → /compute_ik
                                               [3] look_down.py
                                               [7] lerobot_data_harvester.py  ← 最后开
                                                     └── moveit_auto_grasp + 50Hz 录 npz
[8] rqt_image_view（可选）
```

---

### 3.2 各终端完整命令（复制粘贴）

#### 3.2.1 联调前检查清单

- [ ] Orin ↔ NUC 互 ping 通
- [ ] 水瓶在桌面有效空域（x∈[0.38,0.52]，左右 y 分区）
- [ ] 夹爪 launch 后标定正常（见 `20.gripper_issue.md`）
- [ ] **没有**同时跑 `vla_bt_daemon` / `mcp_http_bridge` / 两个 harvester
- [ ] **没有** Ctrl+Z 挂起的僵尸进程（见 §12）
- [ ] NUC 磁盘足够（100 ep × ~3GB 若 `--no-compress`）

**NUC 公共前缀（终端 1 / 2 / 3 / 4 / 7，均为 root）：**

```bash
ssh lab@192.168.26.1
sudo su
cd /home/lab/kuavo-ros-opensource
source devel/setup.bash
```

---

#### 3.2.2 终端 1 — WBC 站起来（NUC · root）

```bash
roslaunch humanoid_controllers load_kuavo_real.launch cali:=true
```

**等待**：

- 标定流程跑完
- 关节「滴」一声
- 机器人**站稳**，不晃

> 没站稳就开 harvester，IK/轨迹容易失败。

---

#### 3.2.3 终端 2 — MoveIt move_group（NUC · root）

```bash
cd /home/lab/kuavo-ros-opensource
source devel/setup.bash
roslaunch kuavo_arm_moveit_config move_group.launch
```

**等待**：

```text
[INFO] MoveGroup context initialization complete
You can start planning now!
```

**验证**（另开终端）：

```bash
source /home/lab/kuavo-ros-opensource/devel/setup.bash
rosservice list | grep compute_ik
# 应有: /compute_ik
```

> ⚠️ **LeRobot 必须有 `/compute_ik`**。  
> 22.2 BT 通道终端 2 写的是 `ik_node.launch`；**LeRobot / moveit_auto_grasp 走 move_group.launch**。  
> 话题 `/move_group/*` 存在 ≠ action server 可用；harvester 已改为 **IK 服务路径**，不硬依赖 MoveGroup action。

---

#### 3.2.4 终端 3 — 低头锁头（NUC · root）

```bash
cd /home/lab/kuavo-ros-opensource
source devel/setup.bash
python3 src/demo/vla_grasp/look_down.py
```

**等待**：日志提示头部已到**最低位**（与 22.2 一致，稳定 TF2 树）。

---

#### 3.2.5 终端 4 — joint_states 引渡（NUC · root）⭐ 必开

WBC 只发 `/sensors_data_raw`，**不发**标准 `/joint_states`。Harvester 录 state、MoveIt 都要 `/joint_states`。

```bash
cd /home/lab/kuavo-ros-opensource
source devel/setup.bash
python3 src/demo/vla_grasp/kuavo_state_publisher.py
```

**验证**：

```bash
rostopic hz /sensors_data_raw    # WBC 有数据
rostopic echo /joint_states -n 1 # zarm_l1_joint 等非零
```

**漏开后果**：state 全 0、IK 异常、遥测不对。

---

#### 3.2.6 终端 5 — 头部相机（Orin · 本地）

```bash
ssh leju_kuavo@192.168.26.12   # 若人在 NUC

cd ~/kuavo_ros_application
source devel/setup.bash
roslaunch dynamic_biped load_robot_head.launch
```

**验证**（NUC 或 Orin 均可）：

```bash
rostopic hz /camera/color/image_raw
# 应有稳定频率（如 30Hz）
```

**常见报错**（相机未就绪则 episode 帧数 0）：

```text
Format MJPG convert to RGB888 failed
Decode frame failed
```

→ 重启终端 5，检查 RealSense 连接与标定文件。

---

#### 3.2.7 终端 6 — YOLO + TF2（Orin · 本地）

```bash
cd ~/kuavo_ros_application
source devel/setup.bash
python3 src/ros_vision/detection_industrial_yolo/yolo_box_object_detection/scripts/yolo_box_segment_ros_TF2.py
```

**验证**：

```bash
rostopic hz /vla/yolo_target
rostopic echo /vla/yolo_target -n 3
# 应持续刷 TF2 坐标，瓶在视野内时 X≈0.45~0.55
```

---

#### 3.2.8 终端 7 — LeRobot 收割机（NUC · root）⭐ 最后开

**确认终端 1~6 都就绪后**：

```bash
cd /home/lab/kuavo-ros-opensource
source devel/setup.bash

# 推荐：25Hz + 无压缩写盘（快 5~10 倍）
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only \
    --episodes 10 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
    --fps 25 \
    --no-compress


#继续采集数据（前面已经采集了30次，所以从编号30开始采集，以此类推）
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only --episodes 10 \
    --start-index 30 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
    --fps 25 --no-compress
```

**⚠️ 务必写绝对路径 `--output-dir /home/lab/...`**。root 下默认 `~/` → `/root/`，scp 会找不到。

**冒烟（先 1 ep）**：

```bash
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only \
    --episodes 1 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw \
    --fps 25 \
    --no-compress \
    --reposition-pause 10
```

**同一时刻只跑一个 harvester**（`pgrep -af lerobot_data_harvester` 应只有 1 条）。

---

#### 3.2.9 终端 8 — RQT 看图（Orin · 可选）

```bash
source /opt/ros/noetic/setup.bash
rqt_image_view
# 选 /camera/color/image_raw
```

Orin 内存紧时可不开；可在终端 5 相机起来后任意时刻开。

---

#### 3.2.10 采数前一键检查（NUC 新开终端）

```bash
source /home/lab/kuavo-ros-opensource/devel/setup.bash

echo "=== IK ==="
rosservice list | grep compute_ik

echo "=== 关节 ==="
rostopic hz /joint_states

echo "=== 相机 ==="
rostopic hz /camera/color/image_raw

echo "=== YOLO ==="
rostopic hz /vla/yolo_target

echo "=== 僵尸 ==="
pgrep -af lerobot_data_harvester
rosnode list | grep move_group_commander_wrappers
```

四项 hz 都有、无多余 harvester/wrapper → 可以开 **终端 7**。

---

<a id="ch4"></a>
## 第 4 章：Episode 管理与批量采数（Day 2）

> 续采、删失手、重建 jsonl、批量踩坑。

### 4.1 Episode 生命周期与人工操作

#### 4.1.1 单条 Episode 时间线

```text
Episode N 开始
    ↓ YOLO 10 帧 + 抓取 ~25s
    ↓ 日志：「松开夹爪释放水瓶！大功告成！」
    ↓ 静默写 npz（--no-compress 约 3~5s；压缩模式 3~10min）
    ↓ 日志：「Episode M 成功 → raw episodes/ep_XXXXXX.npz (K 帧)」
    ↓ 「等待 10.0s — 请将水瓶移动到新位置...」（默认 `--reposition-pause`）
Episode N+1 开始
```

**写盘期间无日志 ≠ 死机**；看 `ls -lh episodes/` 文件是否变大。

#### 4.1.2 失手数据删除

脚本**不判断**是否抓到瓶，只按帧数保存。水平没夹到、瓶滑脱 → **打包前删掉**：

```bash
rm /home/lab/lerobot_kuavo_grasp_raw_batch2/episodes/ep_000003.npz
# 编辑 meta/episodes.jsonl 删掉对应行
```

模仿学习只保留成功示范。

#### 4.1.3 续采不覆盖

同目录重跑会从 `ep_000000` 覆盖。续采用**新目录**：

```bash
--output-dir /home/lab/lerobot_kuavo_grasp_raw_batch3
```

#### 4.1.4 数据量建议

| 规模 | 用途 |
|------|------|
| 1~5 ep | 通路冒烟 |
| 20~50 ep | 小规模 fine-tune |
| 100+ ep | 较稳 VLA 泛化 |

---
#### 4.1.5 实机日志片段

**Episode 成功 + 写盘**：

```text
👐 抵达安全空域，松开夹爪释放水瓶！大功告成！
[INFO] 正在写入 ep_000000.npz (615 帧，1280×800，无压缩)...
[INFO] 写入完成，耗时 4.2s
[INFO] Episode 0 成功 → raw episodes/ep_000000.npz (615 帧 @ 25Hz)
[INFO] 等待 10.0s — 请将水瓶移动到新位置...
[INFO] ========== Episode 2 / 10 ==========
```

**IK 走 service 路径（正常）**：

```text
[INFO] IK 服务已就绪: /compute_ik
[WARN] MoveGroupCommander 不可用 (...)。→ 改用 /compute_ik 服务路径
[INFO] ✅ [左手] 终点切入 IK 成功 (service / zarm_l7_end_effector)
```

---


### 4.2 批量采数实机补充（2026-06-28）


> **说明**：自 §9.2 文档定稿后的实机记录。**不修改上文各章**，以本节为准补充续采、删坏数据、传 raw、打包等操作。  
> **称呼**：**下位机** = `192.168.26.1`（采数、raw 落盘）；**上位机** = `192.168.26.12`（打包、LeRobot 验证）。上下位机均在机器人机体内，**机器人断电则两台都关**，传 raw 须上电且网络通。
#### 4.2.1 续采：`--start-index`（同目录追加）

同 `--output-dir` 重跑 harvester **默认从 `ep_000000` 覆盖**。续采须指定下一档空闲序号：

```bash
# 例：已有 ep_000000 ~ ep_000029（30 个文件），下一批从 30 起
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only --episodes 10 \
    --start-index 30 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
    --fps 25 --no-compress
```

| 注意 | 说明 |
|------|------|
| **`--start-index` 只决定新 npz 文件名** | 与 jsonl 里 `episode_index` 无关，采完需重建 jsonl（§4.2.4） |
| **同一 `start-index` 跑两次** | 第二次会**覆盖**同名 npz，不会多出 10 条 |
| **`--reposition-pause`** | 脚本默认已改为 **10s**，可省略 |

**推荐批量命令（2026-06 实机）**：

```bash
python3 src/demo/vla_grasp/mcp/lerobot_data_harvester.py \
    --raw-only --episodes 10 \
    --start-index 30 \
    --output-dir /home/lab/lerobot_kuavo_grasp_raw_batch2 \
    --fps 25 --no-compress
```
#### 4.2.2 写盘时间（`--no-compress`）

实机 **~3~5 秒/ep**（非旧文档「30~90 秒」）。日志示例：

```text
[INFO] 正在写入 ep_000030.npz (619 帧，1280x800，无压缩)...
[INFO] 写入完成，耗时 3.8s
```

写盘期间终端无新输出属正常；可看 `ls -lh episodes/` 文件是否增大。
#### 4.2.3 抓取 TCP 微调（`moveit_auto_grasp.py`）

失手多因偏爪，在 `moveit_auto_grasp.py` 调 `TCP_OFFSET_*`（harvester / MCP 共用）：

| 参数 | 当前值（2026-06-28） | 含义 |
|------|---------------------|------|
| `TCP_OFFSET_X_LEFT` | `-0.013` | 左手略往前（偏前则更负） |
| `TCP_OFFSET_Y_LEFT` | `0.004` | 左手略往右（偏左则减小） |
| `TCP_OFFSET_X_RIGHT` | `-0.022` | 右手略往前（偏后时增大该值） |
| `TCP_OFFSET_Y_RIGHT` | `0.065` | 右手往左修 |
| `LIFT_HEIGHT_FALLBACKS_M` | `(0.22, 0.18, 0.14, 0.10)` | 抬升 IK 无解时按高度递减重试（m） |

微调步长建议 **1~2mm（0.001~0.002 m）**，每次只改一个方向，采 2~3 条验证。
#### 4.2.4 失手删除 + 重建 `episodes.jsonl`（必做）

脚本**只检查帧数**，不判断是否真的夹到瓶。失手仍会保存 npz，**打包前必须删**。

**步骤 A — 删 npz**：

```bash
rm -v /home/lab/lerobot_kuavo_grasp_raw_batch2/episodes/ep_000027.npz
rm -v /home/lab/lerobot_kuavo_grasp_raw_batch2/episodes/ep_000028.npz
```

**步骤 B — 重建 jsonl**（删 npz 后**每次都要跑**，否则 jsonl 行数与磁盘不一致）：

```bash
python3 <<'EOF'
import json, glob, os
raw = "/home/lab/lerobot_kuavo_grasp_raw_batch2"
meta_path = os.path.join(raw, "meta", "episodes.jsonl")

by_file = {}
if os.path.isfile(meta_path):
    with open(meta_path, encoding="utf-8") as f:
        for line in f:
            if line.strip():
                rec = json.loads(line)
                by_file[rec["file"]] = rec

paths = sorted(glob.glob(os.path.join(raw, "episodes", "ep_*.npz")))
records = []
for i, path in enumerate(paths):
    rel = "episodes/" + os.path.basename(path)
    rec = by_file.get(rel, {"task": "grasp bottle", "fps": 25, "num_frames": 0})
    rec = dict(rec)
    rec["episode_index"] = i
    rec["file"] = rel
    records.append(rec)

with open(meta_path, "w", encoding="utf-8") as f:
    for rec in records:
        f.write(json.dumps(rec, ensure_ascii=False) + "\n")

print("npz:", len(paths), "| jsonl:", len(records))
for r in records:
    side = "左" if r.get("is_left_arm") else "右"
    print("  [%02d] %s  %s" % (r["episode_index"], os.path.basename(r["file"]), side))
EOF
```

**步骤 C — 确认两边一致**：

```bash
ls /home/lab/lerobot_kuavo_grasp_raw_batch2/episodes/*.npz | wc -l
wc -l /home/lab/lerobot_kuavo_grasp_raw_batch2/meta/episodes.jsonl
# 两个数字必须相等
```

jsonl 里的 `episode_index` 会重排为 `0 ~ N-1`；**文件名**可带缺口（如缺 009、010），pack 按 jsonl + 实际 npz 读，无影响。
#### 4.2.5 条数怎么算（为何不是 35 = 17+20）

| 误区 | 实际 |
|------|------|
| 「删 2 条后 17 条，再采 20 次 = 37」 | 每次 `--episodes 10` 只加 **10 个文件** |
| 「同一 `--start-index 20` 跑两次 = +20」 | 第二次**覆盖** ep_000020~029，净增 0 |
| 正确累加 | **磁盘上 `ep_*.npz` 个数** = 有效条数 |

**batch2 实机账目（2026-06-28）**：

```text
第 1~2 批 + 删坏数据  →  25 条有效
第 3 批 start-index 30、10/10 成功  →  +10
合计  →  35 条（~62GB raw）
```

### 4.3 raw npz 格式

```text
lerobot_kuavo_grasp_raw_batch2/
├── meta/
│   ├── dataset_info.json
│   └── episodes.jsonl
└── episodes/
    └── ep_000000.npz   # images (T,800,1280,3), state (T,14), action (T,14)
```

---

### 4.4 采数踩坑（NUC 实机记录）

| 现象 | 原因 | 处理 |
|------|------|------|
| Episode 2 只有夹爪动 | ep1 结束 `arm_traj_change_mode(0)`，ep2 未切回 mode 2 | 已修：每 ep 前 `_ensure_arm_trajectory_mode()` |
| `move_group action server` 超时 | 僵尸 wrapper / action 僵死 | 清僵尸 §12；harvester 回退 `/compute_ik` |
| `compute_ik unavailable` | move_group 挂了或两个 harvester 互抢 | 重启终端 2；只留一个 harvester |
| 写盘 3~10min 无输出 | zlib 压缩 | `--no-compress` |
| Ctrl+C 无效 | 卡在 C 扩展压缩 | `--no-compress` + 子进程写盘；**勿 Ctrl+Z** |
| 数据在 `/root/` | root 默认 `~/` | `--output-dir /home/lab/...` |
| Episode 帧数 0 | 相机 decode 失败 | 重启 **终端 5** |
| 失手 ep | 脚本不判抓取成功 | 打包前手动删 npz + jsonl 行 |

---

### 4.5 停止与僵尸进程清理

#### 4.5.1 正确停止

- **Ctrl+C** 停 **终端 7** harvester（不要用 **Ctrl+Z**）
- 先停终端 7，再停终端 1 WBC

#### 4.5.2 Ctrl+Z 危害

挂起进程不释放 ROS 节点 → `move_group_commander_wrappers_*` 堆积 → move_group 僵死。

#### 4.5.3 清理命令

```bash
# 1. 各终端查挂起任务
jobs -l
kill %1

# 2. 杀 harvester
pkill -9 -f lerobot_data_harvester
pgrep -af lerobot_data_harvester   # 应无输出

# 3. 杀 wrapper 节点
rosnode list | grep move_group_commander_wrappers | while read n; do rosnode kill "$n"; done

# 4. 仍不干净 → 终端 2 Ctrl+C 后重开 move_group.launch
```

#### 4.5.4 清理后重启顺序

**终端 2** → **终端 4** → **终端 5 / 6** → **终端 7**

---

---

<a id="ch5"></a>
## 第 5 章：raw 传输与 Orin 打包

> raw 传到上位机 → conda 环境 → pack → 验证。

### 5.1 raw 传到上位机（可中断、须上电）

> 机器人上电后，上位机 rsync 拉 raw。

机器人**断电则上下位机都关**，无法传文件；**上电后**在上位机执行：

```bash
rsync -avP lab@192.168.26.1:/home/lab/lerobot_kuavo_grasp_raw_batch2/ \
    ~/lerobot_kuavo_grasp_raw_batch2/
```

- `Ctrl+C` 可随时停；**再跑同一条命令**即断点续传（优于 `scp -r`）。
- 另拷 pack 脚本：`rsync -avP lab@192.168.26.1:.../mcp/lerobot_pack_dataset.py ~/`
- 传完验证：`ls ~/lerobot_kuavo_grasp_raw_batch2/episodes/*.npz | wc -l` 应为 **35**，`du -sh` 约 **62G**。

---


### 5.2 Orin 打包流程


> **说明**：自 §4.2.4 jsonl 重建之后的实机记录；**训练主文档见正文 §7**，本节 §6.1~§6.2 为传输细节，§7.11 与 §7.1~§7.10 内容重复，可跳过。  
> **称呼**：**下位机** `192.168.26.1` · **上位机** `192.168.26.12` · **自己电脑** = 外接 NVIDIA GPU 训练机（不在机器人体内）。
#### 5.2.0 上位机 conda 首次安装（Miniconda 未装时）

若 `conda: command not found`，说明上位机尚未装 Miniconda。先查架构：

```bash
uname -m    # Jetson 常见 aarch64；x86 PC 为 x86_64
```

**aarch64（上位机常见）**：

```bash
cd ~
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-aarch64.sh -O miniconda.sh
bash miniconda.sh -b -p $HOME/miniconda3
source ~/miniconda3/etc/profile.d/conda.sh
conda init bash && source ~/.bashrc
```

然后按 §5.2.1 创建环境：

```bash
conda create -n lerobot python=3.12 -y
conda activate lerobot
pip install lerobot opencv-python-headless
unset PYTHONPATH
python -c "from lerobot.datasets.lerobot_dataset import LeRobotDataset; print('ok')"
```

---

#### 5.2.1 一次性 conda 环境

```bash
# Orin
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
conda create -n lerobot python=3.12 -y
conda activate lerobot
pip install lerobot opencv-python-headless
unset PYTHONPATH
python -c "from lerobot.datasets.lerobot_dataset import LeRobotDataset; print('ok')"
```

#### 5.2.2 NUC → Orin 传输

```bash
# Orin 上
scp -r lab@192.168.26.1:/home/lab/lerobot_kuavo_grasp_raw_batch2 ~/
scp lab@192.168.26.1:/home/lab/kuavo-ros-opensource/src/demo/vla_grasp/mcp/lerobot_pack_dataset.py ~/
```

#### 5.2.3 打包命令

```bash
conda activate lerobot
unset PYTHONPATH
export PYTHONUNBUFFERED=1
rm -rf ~/lerobot_kuavo_grasp

python3 ~/lerobot_pack_dataset.py \
    --raw-dir ~/lerobot_kuavo_grasp_raw_batch2 \
    --output-dir ~/lerobot_kuavo_grasp \
    --overwrite \
    --no-video \
    --frame-stride 2
```

#### 5.2.4 加载验证

```bash
python -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
root = str(Path('~/lerobot_kuavo_grasp').expanduser())
ds = LeRobotDataset(repo_id='local/kuavo_vla_grasp', root=root)
print('frames:', len(ds))
print('image:', ds[0]['observation.images.head'].shape)
"
```

**不要用** `root='~/...'`（Python 不展开 `~` → HuggingFace 401）。

**实机 PASS**：`frames: 615`，`image: torch.Size([3, 800, 1280])`，`state: torch.Size([14])`。

---
#### 5.2.5 打包太慢怎么办

**现象**：`Map 310/310` 约 1 秒/ep（读 npz 快），但 **`save_episode` 写 Parquet 很慢**（全分辨率 1280×800，上位机 CPU 弱，**10~30 分钟/ep** 都常见）。

**推荐**：打包时缩小图像（脚本已支持 `--resize`）：

```bash
python3 ~/lerobot_pack_dataset.py \
    --raw-dir ~/lerobot_kuavo_grasp_raw_batch2 \
    --output-dir ~/lerobot_kuavo_grasp_batch2 \
    --overwrite \
    --no-video \
    --frame-stride 2 \
    --resize 400 640
```

| 模式 | 上位机 pack 速度 | 训练 |
|------|------------------|------|
| 全分辨率 1280×800 | 很慢（35 ep 可能十数小时） | 画质最好 |
| **`--resize 400 640`** | 通常 **1~3 分钟/ep** | 模仿学习一般够用 |

若已用全分辨率 pack 完成且验证通过，**不必重 pack**，可直接用于 PC 训练。

**更新 pack 脚本**（下位机仓库最新版含 `--resize` 与 save 耗时日志）：

```bash
rsync -avP lab@192.168.26.1:/home/lab/kuavo-ros-opensource/src/demo/vla_grasp/mcp/lerobot_pack_dataset.py ~/
```

`torchcodec` 警告可忽略，会自动 fallback 到 `pyav`。

---
#### 5.2.6 打包是干什么的

| 阶段 | 产物 | 作用 |
|------|------|------|
| 下位机采数 | raw `ep_*.npz`（~62GB） | 专家示范原始录像 |
| 上位机 pack | `lerobot_kuavo_grasp_batch2/` | **LeRobot 标准格式**，训练框架能直接读 |
| 自己电脑训练 | `outputs/train/...` | 学出策略 checkpoint |
| 实机部署（后续） | 推理节点 | 用模型抓瓶，替代纯硬编码轨迹 |

**训练不需要 raw**，只拷 **pack 后的文件夹**。

---
#### 5.2.7 35 条之后：上位机打包 → 验证 → 训练

**1. 打包**（上位机 conda `lerobot` 环境，§5.2.1）：

```bash
conda activate lerobot
unset PYTHONPATH
export PYTHONUNBUFFERED=1

python3 ~/lerobot_pack_dataset.py \
    --raw-dir ~/lerobot_kuavo_grasp_raw_batch2 \
    --output-dir ~/lerobot_kuavo_grasp_batch2 \
    --overwrite \
    --no-video \
    --frame-stride 2
```

**2. 加载验证**：

```bash
python3 -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
root = str(Path('~/lerobot_kuavo_grasp_batch2').expanduser())
ds = LeRobotDataset(repo_id='local/kuavo_vla_grasp', root=root)
print('frames:', len(ds))
print('image:', ds[0]['observation.images.head'].shape)
print('state:', ds[0]['observation.state'].shape)
"
```

**3. 训练**：见正文 **§7**（拷 pack 文件夹 → PC 装环境 → `lerobot-train` ACT）。35 条可试训，正式效果建议 **50+** 条后再 pack 一版。

| 阶段 | 状态（2026-06-28） |
|------|:------------------:|
| batch2 35 ep raw 采数 | ✅ |
| `--start-index` / 删坏数据 / jsonl 重建 SOP | ✅ 本节 |
| 上位机 pack + 加载验证 | ⬜ 下一步 |
| 外接 GPU VLA 训练 | ⬜ |
#### 5.2.8 加载验证（batch2 实机 PASS）

```bash
conda activate lerobot
unset PYTHONPATH

python3 -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
root = str(Path('~/lerobot_kuavo_grasp_batch2').expanduser())
ds = LeRobotDataset(repo_id='local/kuavo_vla_grasp', root=root)
print('frames:', len(ds))
print('image:', ds[0]['observation.images.head'].shape)
print('state:', ds[0]['observation.state'].shape)
"
```

**实机结果（2026-06-29）**：

| 项 | 值 |
|----|-----|
| episodes | **35** |
| frames | **10770** |
| image | `torch.Size([3, 800, 1280])` |
| state | `torch.Size([14])` |

→ **pack 链路打通**，可进入 PC 训练。

---
#### 5.2.9 为何日志里出现「Episode 36」但总共 35 条

- **35** = 磁盘上 **`.npz` / jsonl 行数**（有效条数）。
- **`ep_000036.npz` / `Episode 36`** = **文件名或 jsonl 里旧的 `episode_index`**（续采 `--start-index 30` 时产生的序号），**不是**「一共有 36 条」。

pack 日志若显示 `打包完成: 35 episodes` → 正确。想统一编号可在下位机跑 **§4.2.4** 重建 jsonl（**不必**为此重 pack）。

---

### 5.3 打包踩坑（Orin）

| 现象 | 处理 |
|------|------|
| `FileExistsError` | 加 `--overwrite`；旧脚本 pre-mkdir 已修 |
| OOM `Killed` | `--no-video --frame-stride 2` |
| 打包看似卡住 | `save_episode` 写 Parquet 5~15min |
| 验证 401 | `Path('~/...').expanduser()` |

---

---

<a id="ch6"></a>
## 第 6 章：pack 数据集拷到自己电脑

> 经下位机中转或 tar 压缩传输；解压后验证 parquet 数量。

> 只拷 pack 文件夹，不拷 raw。
### 6.1 数据集拷到自己电脑（经下位机中转）

**只拷 pack 结果，不拷 raw：**

| 文件夹 | 是否拷到 PC |
|--------|:-----------:|
| **`lerobot_kuavo_grasp_batch2`** | ✅ |
| `lerobot_kuavo_grasp_raw_batch2`（~62GB） | ❌ |

**路径 A：下位机 → 自己电脑（PC 能 SSH 下位机）**

```bash
# 在自己电脑上
rsync -avP lab@192.168.26.1:/home/lab/lerobot_kuavo_grasp_batch2/ \
    ~/kuavo_grasp_lerobot/
```

**路径 B：上位机 → 下位机 → U 盘 / PC**（机器人须上电才能网传）

```bash
# 1) 下位机 pull 上位机
rsync -avP leju_kuavo@192.168.26.12:~/lerobot_kuavo_grasp_batch2/ \
    /home/lab/lerobot_kuavo_grasp_batch2/

# 2) U 盘或 PC 再拷走 /home/lab/lerobot_kuavo_grasp_batch2/
```

**拷完验证**（PC 或下位机）：

```bash
du -sh ~/kuavo_grasp_lerobot   # 远小于 62GB
python3 -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
ds = LeRobotDataset('local/kuavo_vla_grasp', root=str(Path('~/kuavo_grasp_lerobot').expanduser()))
print('frames:', len(ds))
"
# 期望 frames: 10770
```

---
### 6.2 传输太慢：先压缩再拷

Parquet 已有压缩，zip/tar.gz **体积未必小很多**，但**单文件**更好传（U 盘、网盘）。

**在下位机打包压缩：**

```bash
cd /home/lab
tar czvf lerobot_kuavo_grasp_batch2.tar.gz lerobot_kuavo_grasp_batch2
# 或 zip -r lerobot_kuavo_grasp_batch2.zip lerobot_kuavo_grasp_batch2
```

**在自己电脑解压：**

```bash
mkdir -p ~/kuavo_grasp_lerobot
df -h .   # ⚠️ 解压前务必留 ≥15G 空闲（88 ep 轻量化 pack 解压后约 9G）
tar xvf lerobot_kuavo_grasp_batch2.tar.gz -C ~/kuavo_grasp_lerobot/
# 解压后 root 通常是 ~/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2

# 验证完整（必须 88 个 parquet + meta/）
ls lerobot_kuavo_grasp_batch2/data/chunk-000/file-*.parquet | wc -l   # 期望 88
ls lerobot_kuavo_grasp_batch2/meta/info.json                            # 必须存在
```

> **踩坑（2026-06-30 实测）**：PC 磁盘 100% 满时 `tar` 会中途报 **`设备上没有空间`**，只解出一半 parquet（如 43/88），**meta/ 也可能缺失**，LeRobot 无法 load。删不完整目录后 **腾空间再解压**；`my_data.tar` 约 9.2G，解压后约 9G，**tar 体积≈解压体积**（parquet 难再压）。

若 PC 能 SSH 下位机，也可直接 `rsync -avzP`（在线压缩，不占双倍磁盘）。

---

---

<a id="ch7"></a>
## 第 7 章：在自己电脑 GPU 训练（LeRobot ACT）

> **本章是训练主文档**。GPU 环境 → ACT 试训 → 监控 → 离线 MAE 评估。打包/传输踩坑见 **§5**、**§6**。  
> **三台机器分工**：详见 **§1.5**。

### 7.1 训练是什么（原理速览）

| 问题 | 答案 |
|------|------|
| **是模仿学习吗？** | **是。** 专家示范 + **行为克隆**；学「看到类似画面/状态时，输出和示范接近的 14 轴关节动作」。**不是**强化学习（无 reward、无训练时反复试错）。 |
| **用什么训？** | **PyTorch + LeRobot + ACT 策略** + pack 数据集 |
| **是神经网络吗？** | **是。** ACT = CNN 看图像 + Transformer 预测一段未来动作（action chunk） |
| **必须用 GPU 吗？** | **强烈建议必须。** `--policy.device=cuda`；CPU 极慢，不实用 |
| **8GB 显存够吗？** | **够跑 ACT 试训。** 全分辨率 1280×800 建议 `--batch_size=4`；OOM 再改 `2`。SmolVLA 需 **≥16GB**（§7.8） |

**ACT 在学什么（直觉）：**

```text
输入：头顶相机图 (1280×800) + 当前 14 轴关节角
      ↓  神经网络（CNN + Transformer）
输出：接下来一段关节动作（与示范 action 对齐，最小化 loss）
```

### 7.2 把 pack 数据集拷到自己电脑

**只拷 pack 结果，不要拷 raw（~62GB 用不上）：**

| 文件夹 | 拷到 PC？ |
|--------|:---------:|
| **`lerobot_kuavo_grasp_batch2/`**（含 `meta/`、`data/`） | ✅ |
| `lerobot_kuavo_grasp_raw_batch2/` | ❌ |

**路径 A：自己电脑能 SSH 下位机**（推荐，机器人须上电）

```bash
# 在自己电脑上执行
mkdir -p ~/kuavo_grasp_lerobot
rsync -avP lab@192.168.26.1:/home/lab/lerobot_kuavo_grasp_batch2/ \
    ~/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2/
```

**路径 B：上位机 → 下位机 → U 盘 / PC**

```bash
# 下位机上 pull 上位机
rsync -avP leju_kuavo@192.168.26.12:~/lerobot_kuavo_grasp_batch2/ \
    /home/lab/lerobot_kuavo_grasp_batch2/
# 再用 U 盘或 PC rsync 拷走 /home/lab/lerobot_kuavo_grasp_batch2/
```

**路径 C：传输太慢** → 下位机 `tar czvf lerobot_kuavo_grasp_batch2.tar.gz ...`，PC 解压（**§6.2**）。

拷完后目录应类似：

```text
~/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2/
├── meta/
├── data/
└── ...
```

磁盘占用 **远小于 62GB**（Parquet 已压缩）。

### 7.3 一次性环境（自己电脑）

| 项目 | 建议 |
|------|------|
| GPU | NVIDIA，显存 ≥ **8GB**（ACT 试训） |
| 系统 | Ubuntu 22.04 + CUDA 最省事；Windows 可用 WSL2 + CUDA |
| 磁盘 | 数据集 + checkpoint 预留 **20GB+** |

```bash
# Ubuntu / WSL2 示例
conda create -n lerobot python=3.12 -y
conda activate lerobot

# 按 https://pytorch.org 安装带 CUDA 的 torch（版本按你驱动选 cu124/cu121）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

pip install lerobot opencv-python-headless
unset PYTHONPATH

python -c "import torch; print('cuda:', torch.cuda.is_available())"
python -c "from lerobot.datasets.lerobot_dataset import LeRobotDataset; print('lerobot ok')"
```

**必须看到 `cuda: True`**，否则训练会跑在 CPU 上极慢或报错。

### 7.4 训练前：数据集自检

训练时固定两个配置（与 pack 时一致）：

| 配置项 | 值 |
|--------|-----|
| `dataset.repo_id` | **`local/kuavo_vla_grasp`**（本地库，不是 HuggingFace 在线名） |
| `dataset.root` | 指向 **`lerobot_kuavo_grasp_batch2` 文件夹本身** 的绝对路径 |

```bash
conda activate lerobot
unset PYTHONPATH

export DATASET_ROOT="$HOME/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"
# Windows WSL 示例：
# export DATASET_ROOT="/mnt/c/Users/you/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"

python3 -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
root = Path('$DATASET_ROOT').expanduser()
ds = LeRobotDataset('local/kuavo_vla_grasp', root=str(root))
print('frames:', len(ds))
print('episodes:', ds.num_episodes)
print('image:', ds[0]['observation.images.head'].shape)
print('state:', ds[0]['observation.state'].shape)
"
```

**batch2 期望输出（2026-06-29 实机）**：

| 项 | 值 |
|----|-----|
| frames | **10770** |
| episodes | **35** |
| image | `torch.Size([3, 800, 1280])` |
| state | `torch.Size([14])` |

若 `frames` 不对，先在上位机/下位机重跑 §5.2.4 验证，不要急着训练。

```bash
python3 -c "import torch; print(torch.cuda.get_device_name(0))"
df -h .
```

### 7.5 第一次试训：ACT（推荐 baseline）

35 条适合 **smoke train**（验证 loss 能否下降）；要更好抓取效果建议采到 **50~100 条** 再 pack、再训一版。

```bash
conda activate lerobot
unset PYTHONPATH
export DATASET_ROOT="$HOME/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"

lerobot-train \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --policy.type=act \
  --policy.repo_id=local/kuavo_grasp_act \
  --policy.push_to_hub=false \
  --output_dir=outputs/train/kuavo_grasp_act_35ep \
  --job_name=kuavo_grasp_act \
  --policy.device=cuda \
  --steps=50000 \
  --batch_size=4 \
  --save_freq=5000 \
  --log_freq=100
```

> **CLI 参数说明（LeRobot 0.5.x+，实测 2026-06-29）**  
> 训练步数/批大小等用 **顶层 flag**：`--steps`、`--batch_size`、`--save_freq`、`--log_freq`。  
> 旧写法 `--training.offline_steps` / `--training.batch_size` 等会报 **`unrecognized arguments`**。  
> 本地数据集必须加 `--policy.repo_id=...` + `--policy.push_to_hub=false`，否则可能触发 HuggingFace Hub 上传/401。

**8GB 显存**建议 `batch_size=4`（上例已设）；若仍 OOM 改 `2`。12GB+ 可试 `8`。

- 终端 **loss 总体下降** → 正常（前几分钟 GPU 利用率偏低、在编译缓存，属正常）
- checkpoint 目录：`outputs/train/kuavo_grasp_act_35ep/`
- 训练时长因 GPU 而异，**数小时**常见

**若报 `lerobot-train: command not found`**：

```bash
python -m lerobot.scripts.lerobot_train \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --policy.type=act \
  --policy.repo_id=local/kuavo_grasp_act \
  --policy.push_to_hub=false \
  --output_dir=outputs/train/kuavo_grasp_act_35ep \
  --job_name=kuavo_grasp_act \
  --policy.device=cuda \
  --steps=50000 \
  --batch_size=4 \
  --save_freq=5000 \
  --log_freq=100
```

或 `pip show lerobot` 确认已安装（建议 0.5.x+）。

### 7.4.1 batch2 · 88 ep 正式训练（2026-06-30 ~ 07-01 实测）

续采到 **88 ep** 后上位机 **轻量化 pack**（图像 **640×400**，非 raw 1280×800），PC 路径示例：

```text
~/kuavo_all/lerobot_kuavo_grasp_batch2/
├── meta/          # info.json: 88 ep, 27137 frames
├── data/chunk-000/file-000.parquet ~ file-087.parquet  （共 88 个）
└── outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/
```

**第一次训练（batch_size=16，约 20K step 停止）：**

```bash
export DATASET_ROOT="/home/lwy/kuavo_all/lerobot_kuavo_grasp_batch2"

lerobot-train   --dataset.repo_id=local/kuavo_vla_grasp   --dataset.root="$DATASET_ROOT"   --policy.type=act   --policy.repo_id=local/kuavo_grasp_act   --policy.push_to_hub=false   --policy.use_amp=true   --output_dir=outputs/train/kuavo_grasp_act_88ep_bs16   --job_name=kuavo_grasp_act_88ep_bs16   --policy.device=cuda   --steps=50000   --batch_size=16   --num_workers=12   --save_freq=5000   --log_freq=100
```

**从 20K resume 续训到 50K（2026-07-01）：**

```bash
lerobot-train   --dataset.repo_id=local/kuavo_vla_grasp   --dataset.root="$DATASET_ROOT"   --policy.type=act   --policy.repo_id=local/kuavo_grasp_act   --policy.push_to_hub=false   --policy.use_amp=true   --output_dir=outputs/train/kuavo_grasp_act_88ep_bs16   --job_name=kuavo_grasp_act_88ep_bs16   --policy.device=cuda   --resume=true   --config_path=outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/020000/pretrained_model/train_config.json   --steps=50000   --batch_size=16   --num_workers=12   --save_freq=5000   --log_freq=100
```

| 项 | 值 |
|----|-----|
| GPU | RTX 3070 Laptop 8GB |
| batch_size | 16（640×400 下显存约 3~6GB） |
| 续训 30K step 耗时 | 约 17h |
| 50K 终端 loss | 约 **0.023** |
| **部署 checkpoint** | **`checkpoints/050000/pretrained_model/`**（`last -> 050000`） |

> **易混淆**：终端 `smpl:320K` = **sample 数**（20000×16 或 50000×16），**不是 step 数**；`step:20K` 才是步数。

> **output_dir 已存在**：不加 `--resume` 会报 `FileExistsError`；换超参重训用新目录名（如 `_bs16_v2`）或 `rm -rf` 旧目录。

### 7.6 训练成功标准 & 后续

| 里程碑 | 说明 |
|--------|------|
| **试训成功** | PC 上 ACT **loss 正常下降** + **§8 正确 MAE** 可测（88 ep @50K：约 **0.011 rad / 0.6°**） |
| **离线评估** | PC 上对比「模型预测 action vs 专家 action」→ **§8**（不必上真机） |
| 数据不够 | 下位机续采 → 重建 jsonl（§4.2.4）→ 上位机重 pack → PC **重训** |
| 实机部署 | 需另写推理节点（订阅相机、发 arm 指令）— **尚未做**，本文不展开 |
| 真机闭环抓瓶 | 最终验证；需推理节点 + 安全保护 — **尚未做** |

### 7.7 常见报错

| 现象 | 处理 |
|------|------|
| **`unrecognized arguments: --training.xxx`** | 改用顶层 flag：`--steps` / `--batch_size` / `--save_freq` / `--log_freq`（见 §7.5） |
| **HuggingFace 401 / push_to_hub** | 加 `--policy.repo_id=local/kuavo_grasp_act --policy.push_to_hub=false` |
| **CUDA OOM** | `--batch_size=4` 或 `2` |
| `cuda: False` | 重装 **CUDA 版** torch，检查 `nvidia-smi` |
| HuggingFace 401 / 找不到数据集 | `dataset.root` 必须用**绝对路径**；Python 里 `~` 不展开 |
| 训练极慢 | 确认 `--policy.device=cuda`，不是 CPU |
| 想换 SmolVLA | 需更大显存 + 更多数据（50+ ep），见 **§7.8** |

### 7.8 可选：SmolVLA 微调（显存 ≥ 16GB 再试）

```bash
lerobot-train \
  --policy.path=lerobot/smolvla_base \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --output_dir=outputs/train/kuavo_grasp_smolvla \
  --job_name=kuavo_grasp_smolvla \
  --policy.device=cuda
```

35 ep 数据量偏小，SmolVLA 更容易欠拟合；**建议先跑通 ACT**，数据到 50+ 再试。

### 7.9 训练产物：checkpoint 里有什么

训练完成后，`--output_dir` 下大致结构：

```text
outputs/train/kuavo_grasp_act_35ep/
├── checkpoints/
│   ├── 005000/                    # 每 save_freq 步存一次
│   │   ├── pretrained_model/      ← 真正要用的「策略模型」
│   │   │   ├── model.safetensors  # 神经网络权重
│   │   │   └── config.json        # 模型结构 / 超参
│   │   └── training_state/        # 优化器状态（续训用）
│   ├── 010000/
│   └── last/                      # 最新一步（符号链接）
└── wandb/                         # 仅当 --wandb.enable=true 时才有
```

> **注意：** LeRobot **不会**在 `output_dir` 下写 TensorBoard 的 `events.out.tfevents.*` 文件；监控方式见 **§7.10**。

| 产物 | 是什么 | 用途 |
|------|--------|------|
| **`pretrained_model/`** | 训好的 **ACT 策略**（神经网络权重） | 推理：图像+状态 → 14 轴动作 |
| **`training_state/`** | 优化器 / 步数等 | `--resume` 续训 |
| **raw / pack 数据集** | 训练**输入**，不是输出 | 不要和 checkpoint 混淆 |

**当前里程碑（2026-07-01）：** 88 ep 轻量化 pack ✅；ACT **50K step** 训完 ✅；**正确离线 MAE ≈ 0.011 rad（0.6°）** ✅。  
**部署权重：** `outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/050000/pretrained_model/`  
**尚未做：** 实机推理节点（订阅 `/camera`、发 arm 指令）；要真机抓瓶还需后续开发。

### 7.10 如何查看训练效果

#### 7.10.1 三种监控方式（实测 LeRobot 0.5.x+）

| 方式 | 怎么做 | 是否可用 |
|------|--------|:--------:|
| **终端 loss** | 命令里 `--log_freq=100` | ✅ 默认就有 |
| **WandB 曲线** | 加 `--wandb.enable=true --wandb.project=kuavo_grasp` | ✅ **推荐** |
| **TensorBoard** | `tensorboard --logdir outputs/train/...` | ❌ **LeRobot 不写 TB 日志** |

**终端输出示例（每 100 step）：**

```text
step:5K smpl:20K ep:65 epch:1.86 loss:0.842 grdn:12.3 lr:1.0e-05 updt_s:0.12 data_s:0.03
```

**loss 总体下降** 即训练正常。前几分钟 GPU 利用率偏低（编译/缓存）属正常。

> **读日志别搞混**：`step:20K` = 训练步数；`smpl:320K` = 已见 **sample 数**（= step × batch_size，如 20000×16=320000）；`epch:29` = 约 29 遍数据集。`smpl:320K` **不是 32000 step**。

#### 7.10.2 为什么 TensorBoard「打不开 / 是空白页」？

常见有两种情况，不要和「模型坏了」混淆：

| 现象 | 原因 | 处理 |
|------|------|------|
| 浏览器连不上 `localhost:6006` | 远程机器未做 SSH 端口转发 | 本机执行 `ssh -L 6006:localhost:6006 user@服务器IP` 后再开浏览器 |
| TB 能打开但 **No dashboards are active** | LeRobot **训练时不写** `events.out.tfevents.*`；`output_dir` 里只有 `checkpoints/` | **改用 WandB**（§7.10.3），或只看终端 loss |
| `tensorboard: command not found` | `lerobot` conda 环境未装 tensorboard | `pip install tensorboard`（装后也只能看 WandB 导出的日志，LeRobot 本身仍不写 TB） |

**自检：目录里有没有 TB 日志？**

```bash
find outputs/train/kuavo_grasp_act_35ep -name 'events.out.tfevents.*'
# 若为空 → TensorBoard 无数据可看，这是预期行为，不是 checkpoint 损坏
```

**自检：checkpoint 权重是否完好？**

```bash
python3 -c "
import os
from safetensors.torch import load_file
p = 'outputs/train/kuavo_grasp_act_35ep/checkpoints/005000/pretrained_model/model.safetensors'
if os.path.exists(p):
    sd = load_file(p)
    print('OK: 权重未损坏, 张量数 =', len(sd))
else:
    print('未找到:', p)
"
```

#### 7.10.3 用 WandB 看曲线（推荐）

在 **§7.5 命令末尾**追加：

```bash
  --wandb.enable=true \
  --wandb.project=kuavo_grasp
```

- WandB 上的 **run 名称** = `--job_name`（如 `kuavo_grasp_act`）
- 无网环境：`--wandb.mode=offline`，训练结束后 `wandb sync wandb/offline-run-*`
- 日志目录：`outputs/train/kuavo_grasp_act_35ep/wandb/`

**从 checkpoint 续训并开 WandB（示例：已从 5000 step 存盘）：**

```bash
cd "$DATASET_ROOT"   # 进入含 outputs/ 的数据集根目录
conda activate lerobot
unset PYTHONPATH

lerobot-train \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --policy.type=act \
  --policy.repo_id=local/kuavo_grasp_act \
  --policy.push_to_hub=false \
  --output_dir=outputs/train/kuavo_grasp_act_35ep \
  --job_name=kuavo_grasp_act_wandb \
  --policy.device=cuda \
  --resume=true \
  --config_path=outputs/train/kuavo_grasp_act_35ep/checkpoints/005000/pretrained_model/train_config.json \
  --steps=50000 \
  --batch_size=4 \
  --save_freq=5000 \
  --log_freq=100 \
  --wandb.enable=true \
  --wandb.project=kuavo_grasp
```

> **注意：** 若之前未开 WandB，**已跑完的 5000 step 的 loss 历史无法事后补回**（只在当时终端输出里）。续训后 WandB 从当前 step 开始记录。

#### 7.10.4 若坚持用 TensorBoard

LeRobot 官方训练脚本 **不含** TensorBoard 写入逻辑。若 TB 页面空白，**不是启动命令错了**，而是根本没有 events 文件。  
实际可行方案：**用 WandB**；或在 WandB 网页里查看曲线。

**注意：** 曲线上的 loss 下降只说明「网络在学示范」；最终是否真能抓瓶，还要 **离线评估（§8）→ 真机闭环**。35 ep 更适合 **smoke train**，正式效果建议 **50~100 ep** 再训。


### 7.11 在自己电脑里训练（重点 · 2026-06-29 实机记录）

> 与上文 §7.1~§7.10 相同；若只关心训练，直接读 **§7.1~§7.10** 即可。本节保留 2026-06-29 实机记录上下文。

训练在 **带 NVIDIA 显卡的自己电脑** 上进行；**不要**在上位机/下位机里训（无独显或性能不足）。

#### 7.11.1 硬件与系统

| 项目 | 建议 |
|------|------|
| GPU | NVIDIA，显存 ≥ **8GB**（ACT 试训）；越大越好 |
| 系统 | **Ubuntu 22.04 + CUDA** 最省事；Windows 可用 WSL2 + CUDA |
| 磁盘 | 数据集 + checkpoint 预留 **20GB+** |

#### 7.11.2 一次性环境（PC）

```bash
# Ubuntu / WSL2 示例
conda create -n lerobot python=3.12 -y
conda activate lerobot

# 按 https://pytorch.org 安装带 CUDA 的 torch，例如：
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

pip install lerobot opencv-python-headless
unset PYTHONPATH

python -c "import torch; print('cuda:', torch.cuda.is_available())"
python -c "from lerobot.datasets.lerobot_dataset import LeRobotDataset; print('lerobot ok')"
```

`cuda: True` 才能用 GPU 训练。

#### 7.11.3 数据集路径

假设解压/ rsync 后目录为：

```text
~/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2/
├── meta/
├── data/
└── ...
```

训练时固定两个量：

| 配置项 | 值 |
|--------|-----|
| `dataset.repo_id` | `local/kuavo_vla_grasp`（与 pack 时一致，**不是** HuggingFace 在线库） |
| `dataset.root` | 指向 **`lerobot_kuavo_grasp_batch2` 文件夹本身** 的绝对路径 |

```bash
export DATASET_ROOT="$HOME/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"
# Windows WSL 示例：export DATASET_ROOT="/mnt/c/Users/you/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"
```

#### 7.11.4 第一次试训：ACT（推荐 baseline）

35 条适合 **smoke train**（看 loss 能否下降）；正式效果建议 **50~100 条** 再训一版。

```bash
conda activate lerobot
unset PYTHONPATH
export DATASET_ROOT="$HOME/kuavo_grasp_lerobot/lerobot_kuavo_grasp_batch2"

lerobot-train \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --policy.type=act \
  --policy.repo_id=local/kuavo_grasp_act \
  --policy.push_to_hub=false \
  --output_dir=outputs/train/kuavo_grasp_act_35ep \
  --job_name=kuavo_grasp_act \
  --policy.device=cuda \
  --steps=50000 \
  --batch_size=4 \
  --save_freq=5000 \
  --log_freq=100
```

**说明**（与 §7.5 相同；参数以 §7 为准）：


- 首次运行会编译/缓存，前几分钟 GPU 利用率可能偏低，属正常。
- 训练时间因 GPU 而异，**数小时**常见；看终端 **loss 是否总体下降**。
- checkpoint 在 `outputs/train/kuavo_grasp_act_35ep/`。
- 若报 `lerobot-train: command not found`，试 `python -m lerobot.scripts.lerobot_train`（不是 `lerobot.scripts.train`）或 `pip show lerobot` 确认版本（建议 0.5.x+）。

**显存不足（OOM）时**：

```bash
# batch_size 改 4 或 2
--batch_size=4
```

#### 7.11.5 可选：SmolVLA 微调（需更大显存）

```bash
lerobot-train \
  --policy.path=lerobot/smolvla_base \
  --dataset.repo_id=local/kuavo_vla_grasp \
  --dataset.root="$DATASET_ROOT" \
  --output_dir=outputs/train/kuavo_grasp_smolvla \
  --job_name=kuavo_grasp_smolvla \
  --policy.device=cuda
```

35 ep 数据量偏小，SmolVLA **更容易欠拟合**；建议数据到 50+ 再试。

#### 7.11.6 训练前自检清单

```bash
# 1) 数据集能 load
python3 -c "
from pathlib import Path
from lerobot.datasets.lerobot_dataset import LeRobotDataset
root = Path('$DATASET_ROOT').expanduser()
ds = LeRobotDataset('local/kuavo_vla_grasp', root=str(root))
print('frames', len(ds), 'episodes', ds.num_episodes)
"

# 2) GPU
python3 -c "import torch; print(torch.cuda.get_device_name(0))"

# 3) 磁盘
df -h .
```

#### 7.11.7 训练完成之后

| 步骤 | 说明 |
|------|------|
| **离线评估** | PC 上跑 §8 脚本，看 Action MAE |
| 续训 | loss 仍高或 **正确 MAE**（§8.4）仍 >0.05 rad → `--resume` 续训 |
| 实机部署 | 需另写推理节点订阅 `/camera`、发 `/kuavo_arm_target_poses`（**未做**） |
| 真机闭环 | MAE 足够低 + 推理节点就绪后再做 |

当前里程碑（2026-07-01）：**88 ep 50K step 训完**；**正确离线 MAE≈0.011 rad（0.6°）**；部署 `checkpoints/050000/pretrained_model/`。旧「MAE≈0.37」为 **ACT 错误测法**，见 §8.2。

---
---

<a id="ch8"></a>
## 第 8 章：模型离线评估与实机部署

> **必读**：ACT 离线 MAE 必须用正确测法（§8.2~§8.4），否则 MAE 会从 ~1° 错算成 ~24°。

### 8.1 如何测试模型是否可用（不必先上真机）


> **结论先说：** 不必一上来就上真机。先在 PC 做 **离线 action 对比（MAE）**；**但必须用 ACT 正确测法**（§8.2），否则 MAE 会从 ~1° **错算成 ~24°**，误以为「收敛垃圾」。
>
> **2026-07-01 重要更正：** 本文早期版本（§8.3 旧脚本）的 MAE 评估对 ACT **测法错误**；正确测法下 **50K checkpoint MAE ≈ 0.011 rad（0.6°）**，训练效果 **良好**。旧数字保留作踩坑记录，**请勿再引用**。

#### 8.1.1 测试金字塔（由易到难）

```text
第 0 层：权重能加载              ← safetensors 解包验证（§7.10.2）
第 1 层：PC 离线 MAE（正确测法）  ← §8.2~§8.4，不上真机 ✅
第 2 层：真机开环（只推理不执行）  ← 需推理节点；注意 policy.reset()（§8.6）
第 3 层：真机闭环抓瓶              ← 最终验证，尚未实现
```

| 层级 | 能证明什么 | 不能证明什么 |
|:----:|------------|--------------|
| 0 | 文件未损坏、能 `from_pretrained` | 策略质量 |
| 1 | 推理管线正常、单步 action 贴近示范 | 闭环控制、接触力、真抓瓶 |
| 2 | 实时推理延迟、话题与预处理正常 | 能否完成抓取 |
| 3 | **能不能真抓瓶** | — |

> `lerobot-eval` **不能直接用**：LeRobot 内置环境只有 pusht / aloha 等，**没有 Kuavo gym 环境**。

#### 8.2 ⚠️ ACT 离线 MAE 为什么容易测错（必读）

##### 8.2.1 ACT 的 `select_action()` 不是「每帧独立推理」

LeRobot ACT 配置（本项目的 `config.json`）：

| 参数 | 值 | 含义 |
|------|-----|------|
| `chunk_size` | 100 | 一次预测未来 100 步 action |
| `n_action_steps` | 100 | action 队列长度 |
| `temporal_ensemble_coeff` | null | 未启用 temporal ensembling |

**工作机制：**

```text
select_action(obs) 被调用时：
  若 action 队列为空 → 用当前 obs 推理 → 得到 100 步 action chunk → 入队
  从队列 pop 第 1 步返回

队列非空时 → 直接 pop，不再看当前 obs！
```

这在 **实机闭环**（25Hz 连续调用）中是正确设计：一次推理，执行多步。

##### 8.2.2 错误测法：随机抽帧 + 不 reset（本文旧脚本，勿用）

早期 §8 脚本对 **200 个随机帧** 循环：

```python
# ❌ 错误：ACT 专用评估勿用此写法
for i in random_indices:
    pred = policy.select_action(preprocessor(obs_i))  # 未 policy.reset()
    mae += |pred - gt_i|
```

**为什么 MAE 虚高到 ~0.41 rad（24°）：**

```text
帧 A：队列空 → 用 obs_A 推理 → 返回 chunk_A[0]，队列剩 99 步
帧 B（随机，与 A 无关）：队列非空 → 返回 chunk_A[1]，根本没用 obs_B！
         却拿 chunk_A[1] 去和 gt_B 比 → 风马牛不相及 → MAE 爆炸
```

这 **不是模型差**，是 **评估代码与 ACT 语义不匹配**。

##### 8.2.3 正确测法（三选一）

| 方法 | 适用 | 做法 |
|------|------|------|
| **A. 每帧 `reset()`** | 单步 MAE、随机帧采样 | 每测一帧前 `policy.reset()`，再 `select_action` |
| **B. `predict_action_chunk[:,0]`** | 单步 MAE、**推荐** | 不经过队列，直接取 chunk 第 0 步与 gt 比 |
| **C. 按 episode 顺序调用** | 更接近实机 | 队列不 reset，帧索引连续递增（同一条 episode 内） |

方法 A 与 B 在单步 MAE 上等价；**离线评估推荐 B**（更清晰，不依赖队列状态）。

##### 8.2.4 错误 vs 正确 MAE 对照（同一 50K 模型，500 帧采样）

| 测法 | 5K | 20K | 50K |
|------|:--:|:---:|:---:|
| ❌ 旧脚本（随机帧、不 reset） | ~0.26* | ~0.42 | **~0.41 rad（24°）** |
| ✅ 正确（`reset()` 或 chunk[:,0]） | **0.045 rad（2.6°）** | **0.019 rad（1.1°）** | **0.011 rad（0.6°）** |

\*同一 loop 内错误测法也会波动，均不可信。

##### 8.2.5 为何 loss≈0.023 与错误 MAE≈0.41 同时出现？

| 指标 | 测的是什么 | 可信吗 |
|------|------------|:------:|
| **训练 loss ≈ 0.023** | chunk 上 L1 + VAE KL（与训练目标一致） | ✅ |
| **错误 MAE ≈ 0.41** | 随机帧 + 队列污染后的伪误差 | ❌ |
| **正确 MAE ≈ 0.011** | 单步 action 与专家 action 的 L1 | ✅ |

**结论：loss 低 + 错误 MAE 高 = 评估 bug，不是训练失败。**

#### 8.3 ❌ 错误测法脚本（历史踩坑，保留勿用）

> 以下脚本曾写入本文 §8.2，导致 **88 ep 50K 模型被误判为「收敛垃圾」**。仅作反面教材。

```bash
# ❌ 勿用 — 对 ACT 会得到虚高 MAE
python3 << 'EOF'
import torch, numpy as np
from lerobot.datasets.lerobot_dataset import LeRobotDataset
from lerobot.policies.act.modeling_act import ACTPolicy
from lerobot.policies.factory import make_pre_post_processors

DATASET_ROOT = "/path/to/lerobot_kuavo_grasp_batch2"
CKPT = "outputs/train/.../pretrained_model"
device = torch.device("cuda")
policy = ACTPolicy.from_pretrained(CKPT).to(device).eval()
pre, post = make_pre_post_processors(
    policy_cfg=policy.config, pretrained_path=CKPT,
    preprocessor_overrides={"device_processor": {"device": str(device)}},
)
ds = LeRobotDataset("local/kuavo_vla_grasp", root=DATASET_ROOT)
for i in np.linspace(0, len(ds)-1, 200, dtype=int):
    b = ds[i]
    obs = {k: v for k, v in b.items() if k.startswith("observation")}
    gt = b["action"].float()
    with torch.inference_mode():
        pred = post(policy.select_action(pre(obs))).squeeze(0).cpu()  # ← 未 reset，错！
    ...
EOF
```

#### 8.4 ✅ 正确测法脚本（PC 一键跑，推荐）

```bash
cd "$DATASET_ROOT"    # 例：~/kuavo_all/lerobot_kuavo_grasp_batch2
conda activate lerobot
unset PYTHONPATH

python3 << 'EOF'
import torch
import numpy as np
from lerobot.datasets.lerobot_dataset import LeRobotDataset
from lerobot.policies.act.modeling_act import ACTPolicy
from lerobot.policies.factory import make_pre_post_processors

DATASET_ROOT = "/home/lwy/kuavo_all/lerobot_kuavo_grasp_batch2"   # 改成你的路径
CKPT = "outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/050000/pretrained_model"
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

policy = ACTPolicy.from_pretrained(CKPT).to(device).eval()
preprocessor, postprocessor = make_pre_post_processors(
    policy_cfg=policy.config,
    pretrained_path=CKPT,
    preprocessor_overrides={"device_processor": {"device": str(device)}},
)

ds = LeRobotDataset("local/kuavo_vla_grasp", root=DATASET_ROOT)
indices = np.linspace(0, len(ds) - 1, 500, dtype=int)

maes_reset, maes_chunk = [], []
for i in indices:
    batch = ds[i]
    obs = {k: v for k, v in batch.items() if k.startswith("observation")}
    gt = batch["action"].float()
    proc = preprocessor(obs)
    with torch.inference_mode():
        # 方法 B（推荐）：chunk 第 0 步
        chunk = policy.predict_action_chunk(proc)[:, 0]
        pred_b = postprocessor(chunk).squeeze(0).cpu()
        # 方法 A：reset + select_action（应与 B 一致）
        policy.reset()
        pred_a = postprocessor(policy.select_action(proc)).squeeze(0).cpu()
    maes_chunk.append((pred_b - gt).abs().mean().item())
    maes_reset.append((pred_a - gt).abs().mean().item())

print(f"frames={len(ds)}, episodes={ds.num_episodes}")
print(f"MAE chunk[:,0]: mean={np.mean(maes_chunk):.4f} rad ({np.degrees(np.mean(maes_chunk)):.2f}°)")
print(f"MAE reset():    mean={np.mean(maes_reset):.4f} rad ({np.degrees(np.mean(maes_reset)):.2f}°)")
EOF
```

**指标含义：** 在 N 帧均匀采样上，模型 **当前观测下应输出的第 1 步 action** 与专家 `action` 的 **平均绝对误差（MAE，rad）**。14 轴 joint 误差的均值。

**参考标准（88 ep · 640×400 · 正确测法）：**

| MAE (rad) | 约等于 | 判断 |
|-----------|--------|------|
| < 0.02 | < 1.2° | **很好**（50K 在此区间） |
| 0.02 ~ 0.05 | 1° ~ 3° | 良好，可试真机开环 |
| 0.05 ~ 0.15 | 3° ~ 9° | 一般，建议续训或查数据 |
| > 0.15 | > 9° | 检查预处理 / checkpoint / 测法是否正确 |

#### 8.5 实测记录

##### 8.5.1 batch2 · 35 ep · 1280×800 · 5K step（2026-06-29）

| 项目 | 值 |
|------|-----|
| checkpoint | `kuavo_grasp_act_35ep/checkpoints/005000/pretrained_model` |
| 数据集 | 35 ep / 10770 frames / **1280×800** |
| **错误测法 MAE** | mean=**0.3683** rad（≈21°）← 旧脚本，**不可与 88 ep 直接比** |

> 35 ep 时未意识到 ACT 测法问题；该 0.37 很可能也 **偏高**，但未用正确脚本复测。

##### 8.5.2 batch2 · 88 ep · 640×400 轻量化 · 5K~50K step（2026-07-01）

| 项目 | 值 |
|------|-----|
| 数据集 | **88 ep / 27137 frames** / 640×400 |
| 训练目录 | `outputs/train/kuavo_grasp_act_88ep_bs16` |
| 部署 checkpoint | **`checkpoints/050000/pretrained_model/`** |

**正确 MAE（500 帧采样，`predict_action_chunk[:,0]`）：**

| step | MAE mean (rad) | 约等于 |
|:----:|:--------------:|:------:|
| 5K | 0.045 | 2.6° |
| 10K | — | — |
| 20K | 0.019 | 1.1° |
| 30K | — | — |
| **50K** | **0.011** | **0.6°** |

**错误 MAE（旧脚本，勿引用）：** 20K~50K 均在 **~0.41~0.25 rad** 假象区间，**不代表模型能力**。

**训练曲线摘要：**

| 阶段 | step | 终端 loss | 正确 MAE | 说明 |
|------|:----:|:---------:|:--------:|------|
| 首训 | 5K~20K | 下降中 | 0.045→0.019 | batch16，约 12h 到 20K |
| 续训 | 20K~50K | **≈0.023** | 0.019→**0.011** | resume，约 17h |
| 错误解读 | 20K~50K | loss 低 | 旧 MAE「平台 ~0.42」 | **测法 bug，已更正** |

**为何 88 ep 比 35 ep「错误 MAE」更高？** 多场景数据更难 + **测法错误** 叠加；**正确 MAE 50K=0.011 才是真实水平**。

##### 8.5.3 权重自检

```bash
python3 -c "
from safetensors.torch import load_file
p='outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/050000/pretrained_model/model.safetensors'
sd=load_file(p)
print('OK, tensors:', len(sd))
"
# 期望：234 个张量
```

#### 8.6 实机推理时 `select_action` 注意事项

与离线 **单步 MAE** 不同，实机必须 **连续** 调用 `select_action()`：

```text
每个 episode / 重置后：
  policy.reset()                    ← 清空 action 队列
  loop 25Hz:
    obs = camera + joint_state
    action = policy.select_action(obs)   ← 队列空时才重新推理
    发送 action（开环可先 print）
```

| 场景 | 要不要 reset |
|------|:------------:|
| 新 episode 开始 | ✅ `policy.reset()` |
| 开环/闭环控制循环内 | ❌ 不要每帧 reset |
| 离线单步 MAE 评估 | ✅ 每帧 reset，或用 chunk[:,0] |

#### 8.7 真机测试（最终阶段，尚未做）

| 阶段 | 做法 | 状态 |
|------|------|:----:|
| 开环 | 订阅 `/camera` + 关节状态 → `select_action` → **只打印/录 bag，不发电机** | ⬜ |
| 闭环 | 推理结果发 `/kuavo_arm_target_poses`，真抓水瓶 | ⬜ |

闭环前必须有：**急停、关节限位、慢速试运行、有人看护**。

**PC 离线 MAE 低仍不等于 100% 能抓瓶**（闭环延迟、接触、分布偏移）；但 **0.011 rad 说明模仿学习本身已成功**，值得进入实机开环阶段。

---
---

<a id="ch9"></a>
## 第 9 章：进度与参考

> 编号对照、进度索引、脚本路径。

### 9.1 进度总表（2026-07-01）


| 阶段 | 状态 |
|------|:----:|
| batch2 88 ep raw 采数 + 续采 | ✅ |
| 上位机轻量化 pack（27137 frames / 640×400） | ✅ |
| 数据集 → PC（tar 解压须留 ≥15G） | ✅ |
| PC ACT 训至 50K step（bs16） | ✅ |
| PC 正确离线 MAE ≈ 0.011 rad | ✅ |
| 实机推理 / 闭环抓瓶 | ⬜ |

---


### 9.2 进度与脚本索引


| 阶段 | 状态 |
|------|:----:|
| 1 ep 采 + Orin 打包 + 加载验证 | ✅ |
| `--no-compress` / `--fps 25` / 每 ep mode2 修复 | ✅ |
| batch2 88 ep + 轻量化 pack | ✅ |
| PC ACT 50K + 正确 MAE 评估 | ✅ |
| 实机推理节点 + 开环验证 | ⬜ |
| 实机闭环抓瓶 | ⬜ |

| 脚本 | 路径 |
|------|------|
| 收割机 | [`mcp/lerobot_data_harvester.py`](../kuavo-ros-opensource/src/demo/vla_grasp/mcp/lerobot_data_harvester.py) |
| 打包 | [`mcp/lerobot_pack_dataset.py`](../kuavo-ros-opensource/src/demo/vla_grasp/mcp/lerobot_pack_dataset.py) |
| joint_states | [`kuavo_state_publisher.py`](../kuavo-ros-opensource/src/demo/vla_grasp/kuavo_state_publisher.py) |
| 低头 | [`look_down.py`](../kuavo-ros-opensource/src/demo/vla_grasp/look_down.py) |
| 专家工人 | [`moveit_auto_grasp.py`](../kuavo-ros-opensource/src/demo/vla_grasp/moveit_auto_grasp.py) |

---

### 9.3 与 22.2 / 22.3 编号对照


> 22.4 **按启动顺序** 1~8 编号；22.2 按功能分区编号（终端 5 = 执行入口，在 Orin 相机/YOLO **之前** 就开）。

| 22.4 启动顺序 | 22.4 LeRobot 脚本 | 22.2 BT 对应 | 22.3 MCP 对应 |
|:-------------:|-------------------|--------------|---------------|
| **1** | WBC 站起来 | 终端 1 ✅ | 终端 1 ✅ |
| **2** | `move_group.launch` | 终端 2 `ik_node.launch` ⚠️不同 | move_group ✅ |
| **3** | `look_down.py` | 终端 3 ✅ | 终端 3 ✅ |
| **4** | `kuavo_state_publisher.py` | （22.2 无） | 22.4 **终端 4**（22.3 文档曾称「终端 S」） |
| **5** | `load_robot_head.launch` | 终端 6 ✅ | 终端 6 ✅ |
| **6** | `yolo_box_segment_ros_TF2.py` | 终端 7 ✅ | 终端 7 ✅ |
| **7** | **`lerobot_data_harvester.py`** | 终端 5 `vla_bt_daemon` ⚠️不同 | 终端 5 `mcp_http_bridge` ⚠️不同 |
| **8** | `rqt_image_view`（可选） | 终端 8 ✅ | 可选 |
| — | （采数不需要） | 终端 4 `tts_server` lab | 可选 |
| — | （采数不需要） | 终端 9 语音主控 | `test_mcp_grasp` |

**执行入口三选一**（同一时刻只能开一个）：22.2 终端 5 / 22.3 MCP 桥 / **22.4 终端 7**。

---
---

<a id="ch10"></a>
## 第 10 章：常见问答（2026-06 答疑）

> 本节整理文档使用过程中反复问到的问题；操作细节仍以 **§1.5**、**§7** 为准。

> 本节整理文档使用过程中反复问到的问题；操作细节仍以 **§1.5、§7** 为准。
### 10.1 这是模仿学习吗？和强化学习有什么区别？

**是模仿学习（Imitation Learning / 行为克隆）。**

- 下位机采的是 **专家示范**：机器人按 `moveit_auto_grasp` 成功抓瓶时的「图像 + 状态 + 动作」
- 训练时网络学：**给定观测 → 输出接近示范的 action**
- **不是**强化学习：训练时没有机器人在环境里试错，也没有 reward 函数
### 10.2 训练必须用 GPU 吗？8GB 显存够不够？

| 项目 | 说明 |
|------|------|
| GPU | **强烈建议 NVIDIA + CUDA**；命令 `--policy.device=cuda` |
| CPU | 理论上能跑，**极慢**，不实用 |
| 8GB + ACT | ✅ **够试训**；1280×800 建议 `batch_size=4`，OOM 改 `2` |
| SmolVLA | ❌ 8GB 不够；建议 **≥16GB** 且数据 **50+ ep** |

自检：`python -c "import torch; print(torch.cuda.is_available())"` 须为 `True`。
### 10.3 下位机和上位机各自做什么？

详见 **§1.5**。简记：

- **下位机**：WBC / MoveIt / 执行抓取 / **写 raw npz**
- **上位机**：相机 / YOLO / **pack 成 LeRobot**
- **自己电脑**：**训练**；不拷 62GB raw，只拷 pack 文件夹
### 10.4 训练用到了什么？训完得到什么？

| 阶段 | 用到什么 | 得到什么 |
|------|----------|----------|
| 训练输入 | pack 数据集 + PyTorch + LeRobot + **ACT 神经网络** | — |
| 训练输出 | — | `checkpoints/.../pretrained_model/`（**策略权重**，`.safetensors`） |
| 不是输出 | — | raw npz、pack 文件夹本身（那是输入） |

训完得到的是 **可部署的策略 checkpoint**（神经网络参数），不是新数据集。实机推理节点 **尚未实现**（§7.9）。
### 10.5 能用 TensorBoard 看训练效果吗？

**LeRobot 当前版本（0.5.x+）默认不支持 TensorBoard 曲线。**

| 方式 | 是否可用 | 说明 |
|------|:--------:|------|
| **终端 loss** | ✅ | `--log_freq=100`，最简单 |
| **WandB** | ✅ 推荐 | `--wandb.enable=true --wandb.project=kuavo_grasp` |
| **TensorBoard** | ❌ | 训练不写 `events.out.tfevents.*`；TB 能启动但页面空白是**预期行为** |

详见 **§7.10**（含「TB 空白页」排查、权重自检、续训 + WandB 命令）。

loss 下降 ≠ 一定能抓瓶；先做 §8 离线 MAE，再考虑真机。
### 10.6 文档里训练命令在哪？

| 位置 | 内容 |
|------|------|
| **§1.6.4 Day 3** | 5 步清单 + 最小可复制命令 |
| **§7** | 训练主文档（环境、命令、产物、监控） |
| **§8** | 离线评估模型（不必上真机） |
| §7 | 与 §7 重复，可跳过 |
### 10.7 怎么测试模型能不能用？必须上真机吗？

**不必先上真机。** 推荐顺序见 **§8**：

1. **权重自检**（safetensors 能加载）
2. **PC 离线 MAE**（预测 action vs 专家 action）
3. 续训直到 MAE 明显下降
4. 最后才做真机闭环（需推理节点，**尚未做**）

**必须用 §8.4 正确脚本**测 MAE。88 ep @50K 正确 MAE **≈0.011 rad（0.6°）**；旧 0.37~0.42 为 ACT 测法错误，见 **§8.2**。
### 10.8 为什么 loss 很低（0.023）但 MAE 很高（0.41）？是不是训练失败了？

**不是。2026-07-01 已确认：0.41 是 ACT 离线评估 bug。**

- ACT 的 `select_action()` 内部有 **100 步 action 队列**；对 **随机帧** 连续调用且 **不 `policy.reset()`** 时，返回的是 **上一个观测** 的 chunk 后续步，与当前帧 gt 无关 → MAE 虚高。
- **正确测法**下 50K 模型 MAE **≈ 0.011 rad（0.6°）**，与 loss≈0.023 **一致**。
- 详见 **§8.2 ~ §8.5**（含错误/正确脚本对照表）。

### 10.9 部署用哪个 checkpoint？

| 项目 | 值 |
|------|-----|
| **推荐** | `outputs/train/kuavo_grasp_act_88ep_bs16/checkpoints/050000/pretrained_model/` |
| 图像输入 | **640×400**（与轻量化 pack 一致） |
| 实机相机 | 须在推理节点 **resize 到 640×400**，或使用与训练一致的 preprocessor |
| 旧 35 ep 1280×800 checkpoint | **不兼容** 当前 88 ep 640×400 模型 |

---


*文档版本：22.4 · 2026-07-01 · 10 章重组 · §1 系统架构图 · §8 ACT 离线评估 · §10 答疑*
